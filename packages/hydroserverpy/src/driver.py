# For debugging, uncomment this file and run whatever code you need to test with the vs code python debugger

# from pathlib import Path
# import re
# from dotenv import load_dotenv
# import os
# import pandas as pd
# from hydroserverpy import HydroServer


# load_dotenv()

# hs = HydroServer(
#     host=os.getenv("HS_HOST"),
#     email=os.getenv("HS_EMAIL"),
#     password=os.getenv("HS_PASSWORD"),
# )
